<h1>All Students Within Given Parameters</h1>
<ul>
<% @students.each do |student| %>
  <li>
      <%= link_to student.name, student %>
      <%= render partial: "student", locals: {student: student} %>
  </li>
<% end %>
</ul>